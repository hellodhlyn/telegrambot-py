dist: xenial

language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

before_install:
  - pip install pipenv

install:
  - pipenv install --dev

script:
  - pipenv run lint
  - pipenv run test

after_success:
  - pip install codecov
  - codecov
